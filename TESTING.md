Run the simple tests with:

```cmd
npm test
```

This executes `test/run-tests.ts` using `tsx`. The tests are lightweight and validate core utilities, state save/load, and a few autocomplete helpers.
